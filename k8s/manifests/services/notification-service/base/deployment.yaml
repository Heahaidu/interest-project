apiVersion: apps/v1
kind: Deployment
metadata: 
  name: notification-service
spec: 
  replicas: 1
  strategy: 
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  selector: 
    matchLabels:
      app: notification-service
  template:
    metadata: 
      labels:
        app: notification-service
    spec: 
      containers:
        - name: notification-service
          image: heahaidu/interest.notification-service
          ports:
            - name: http
              containerPort: 8080
