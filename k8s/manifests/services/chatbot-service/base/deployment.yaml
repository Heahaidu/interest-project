apiVersion: apps/v1
kind: Deployment
metadata: 
  name: chatbot-service
spec: 
  replicas: 1
  strategy: 
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  selector: 
    matchLabels:
      app: chatbot-service
  template:
    metadata: 
      labels:
        app: chatbot-service
    spec: 
      containers:
        - name: chatbot-service
          image: heahaidu/interest.chatbot-service
          ports:
            - name: http
              containerPort: 8080
