apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot-dev
  template:
    spec:
      containers: 
        - name: chatbot-service
          image: heahaidu/interest.chatbot-service
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 100m
              memory: 192Mi